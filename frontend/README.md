# Frontend

This folder is reserved for the UI application your partner will build.

Suggested next step:

- Initialize a frontend stack here, such as React, Vite, or Next.js.
- Keep API calls pointed at the backend service defined under `Backend/`.
